# OwnerWeb 开发任务清单

> 本文档是 OwnerWeb 的开发任务清单，用于拆解后续开发工作、标记任务状态，并约束每次任务的执行方式。

## 执行规则

1. 每次只做一个编号任务。
2. 开始前阅读 `memory-bank/` 中相关文档。
3. 不实现任务清单之外的功能。
4. 完成后更新 `memory-bank/PROGRESS.md`。
5. 改动架构或数据字段时，同步更新 `ARCHITECTURE.md` 或 `DATA_MODEL.md`。
6. AI 协作期间不发送图片、截图或非文本消息。
7. AI 不自动打开浏览器；需要查看页面时只提供文本链接。
8. 任何代码、依赖、配置、路由、页面、视觉、接口、数据库、部署或验收范围变化，都必须在任务结束前同步更新对应的 `memory-bank/` 文档。
9. 文档同步是任务完成标准的一部分；只改代码不同步文档，任务不算完成。

## Phase 0：项目卫生与安全基线

- [x] 0.1 将 `vite.config.js` 中 `server.open` 改为 `false`，避免开发服务自动打开浏览器。
- [x] 0.2 增加 `.gitignore`，排除 `node_modules/`、`dist/`、`server/node_modules/`、`server/data/`、`server/uploads/`、`.env`。
- [x] 0.3 增加 `server/.env.example`，只保留变量名和注释，不写真实密钥。
- [x] 0.4 缺少 `JWT_SECRET` 时拒绝启动，移除开发默认密钥。
- [x] 0.5 移除初始管理员的默认弱密码；未显式配置管理员环境变量时不自动创建账号。

## Phase 1：数据与接口加固

- [ ] 1.1 统一 API 响应格式与错误码。
- [x] 1.2 为留言内容增加最大长度限制，限制为 500 字。
- [x] 1.3 项目评论接口校验 `projectId` 必须对应存在的项目。
- [x] 1.4 校验 `parent_id` 必须指向同表、同项目下存在的评论。
- [x] 1.5 公开评论接口不返回 `email` 和 `user_id` 等非公开字段。
- [ ] 1.6 为昵称、头像文件类型和文件大小补充完整校验。
- [x] 1.7 删除用户时将评论 `user_id` 置空，保留昵称和邮箱快照。
- [x] 1.8 为访客留言表和项目评论表增加必要索引和外键策略。
- [x] 1.9 删除旧评论备份表，迁移成功后不再保留 `comments_legacy`。
- [x] 1.10 为访客留言表和项目评论表增加 `root_id`，新评论自动继承顶层评论 ID，历史数据自动回填。
- [ ] 1.11 数据量增长后，基于 `root_id` 实现顶层评论 keyset 分页与线程展开加载。

## Phase 2：前端状态与权限

- [ ] 2.1 为 `/settings` 增加路由守卫或跳转登录页，而不是仅渲染提示。
- [ ] 2.2 为 `/admin` 增加管理员路由守卫。
- [ ] 2.3 处理登录态初始化 loading，避免刷新时短暂显示未登录状态。
- [ ] 2.4 后台删除评论、删除用户、封禁用户前增加二次确认。
- [ ] 2.5 为留言、用户列表和资料保存补充加载状态。
- [ ] 2.6 为接口失败提供更明确的错误展示。
- [x] 2.7 评论头像改为读取用户当前 `avatar` 并渲染真实图片，上传头像后评论头像同步更新。
- [x] 2.8 页面统一命名为账号设置，主路由改为 `/settings`，移除用户个人签名维护入口。

## Phase 3：内容与体验

- [ ] 3.1 确认 `public/videos/yuhu.mp4` 和 `public/videos/zhiyun.mp4` 是否存在。
- [ ] 3.2 为项目视频增加封面图和加载失败提示。
- [ ] 3.3 补充移动端布局检查，重点检查导航、项目卡片、后台表格和表单。
- [ ] 3.4 优化后台表格在移动端的可读性。
- [ ] 3.5 检查并修复所有可交互元素的可访问性标签。
- [ ] 3.6 审查首页视觉动画性能，保留 `prefers-reduced-motion` 支持。
- [x] 3.7 将项目数据迁入 `projects` 表，实现项目增删改、视频/封面上传、后台删除二次确认，并让前端项目列表与详情读取动态数据。
- [x] 3.8 将个人优势迁入 `strengths` 表，增加后台增删改与排序，`/strengths` 页面读取动态数据。
- [x] 3.9 将个人经历、Hero 与联系信息拆为后台可编辑内容，并让首页、`/experience`、联系区读取动态数据。
- [x] 3.10 完善公网部署时的数据库、上传目录、媒体文件代理与验证清单。
- [x] 3.11 修复后台项目管理标题栏布局，保证标题单行显示、新增按钮自适应内容宽度且移动端不溢出。
- [x] 3.12 修复后台项目变更后前端内容未及时刷新的问题，后台保存/上传/删除后同步项目上下文，前端路由切换时重新拉取动态项目。
- [x] 3.13 设计并实现不含 LH 字母的导航栏品牌图标，使用内联 SVG 适配桌面、移动端和悬停状态。
- [x] 3.14 将导航栏品牌图标与站点 favicon 替换为新版 brand-icon.svg 设计，并同步导航容器样式。
- [x] 3.15 为个人介绍页 LH 圆形徽章增加硬币式动效：悬停缓慢旋转、点击快速旋转并精确回正，徽章拆为独立图层并遵循 prefers-reduced-motion。
- [x] 3.16 修复 LH 徽章与旧缓存背景图重叠问题（背景图更名为 portrait-bg.svg），并将徽章重构为双面立体硬币：厚度边缘、随角度变化的动态投影，消除 3D 变换抖动。
- [x] 3.17 为个人介绍页头像卡增加景深背景：硬币后方多层焦外光斑与走线、聚焦光晕，并支持鼠标视差（prefers-reduced-motion 下禁用）。
- [x] 3.18 重做头像卡背景：移除焦外光斑方案，将平面网格升级为一点透视 3D 网格空间（地板/天花板网格流动、硬币位于灭点中心、后墙细网格与红色环境光），背景图更名为 portrait-room.svg 并移除其中平面网格。
- [x] 3.19 头像卡空间改为方形隧道：新增左右网格墙与三重递缩方形线框，后墙改为红色描边的方形网格面板，硬币居中于方形空间灭点。
- [x] 3.20 头像卡空间重做为方形凹腔：移除全部网格线与线框，矩形正面挖出方形开口（切口描边），四壁改为光影渐变平面，深处为深色方形背板与微弱红晕，硬币缩小至 64% 居中悬浮于腔体内。
- [x] 3.21 凹腔扩充至整个矩形卡片：移除中部方形切口与背景图（改用 CSS 渐变底），四壁从卡片四边向内收缩，深色背板与红晕铺满腔底，硬币保持居中悬浮。
- [x] 3.22 修复移除背景图后头像卡整体不显示的问题：为 `.portrait-media` 补充 `aspect-ratio: 4 / 5` 作为高度来源，并清理已失效的 `.portrait-media img` 规则。
- [x] 3.23 硬币位置与交互升级：硬币下移并深入腔体（translateZ 真深度）、悬停方向按鼠标所在侧决定（左区向右转、右区向左转，区域扩大到整块卡片）、支持鼠标拖动旋转并在松手后缓动回正。
- [x] 3.24 硬币深度感与表面 3D 化：硬币加俯仰角与高光/反光，投影改落到腔体地板，四壁提亮并加轻微相机视差；ONLINE 小红点改为沿硬币内部圆环公转（标签保持水平），遵循 prefers-reduced-motion。
- [~] 3.25 硬币改为固定光源的动态反光（被 3.26 取代：CSS 假高光方案放弃）。
- [x] 3.26 将个人介绍页 CSS 硬币替换为 three.js 真实金属硬币（`src/components/Portrait3D.jsx`）：程序化环境贴图产生真实镜面反射、圆角倒角（LatheGeometry），悬停上下俯仰 + 左右旋转、鼠标拖拽旋转、移出回正，反光对齐 Hero PCB 的 PBR 质感。
- [x] 3.27 仓库托管平台可自主选填：后台「身份与联系」增加 Gitee / GitHub / GitCode 三个复选框，勾选后填写对应平台的用户名与链接；前端「联系我」渠道与「个人经历」联系方式按已勾选平台动态渲染并支持点击跳转，兼容旧 `github/githubUrl` 数据。
- [x] 3.28 「联系我」区块标题可编辑：后台增加联系区大标题与小标签字段，`site_content` 增加 `contact_json` 列存储，前端联系区读取动态值并以默认文案兜底。
- [x] 3.29 项目「仅登录用户可查看」：`projects` 增加 `requires_login` 列，后台项目管理增加勾选框，公开接口对未登录用户只返回锁定卡片信息（不含正文/视频/链接），前端锁定卡片显示锁标识、详情页提示登录。
- [x] 3.30 视频占位修复：项目未填写视频链接时详情页不再渲染视频区与「还没有配置演示视频」占位；填写链接后正常播放。
- [x] 3.31 手机端性能优化：新增 `useRenderActive`（元素离屏或页面后台时暂停 WebGL 渲染），Hero PCB 与个人介绍硬币在移动端降低 DPR、关闭抗锯齿、降低几何精度，导航栏毛玻璃在窄屏降低模糊强度。
- [x] 3.32 注册邮箱校验加强：新增 `worker/src/lib/email.js`（严格格式校验、常见拼写错误域名提示、一次性邮箱黑名单、Cloudflare DoH 域名 MX 校验），注册接口接入，前端 `AuthPage` 增加即时格式校验。

## Phase 6：视频对象存储（腾讯云 COS）

- [x] 6.1 控制台准备（用户操作）：创建 COS 桶（公有读私有写）、创建子账号密钥、配置 CORS 允许前端直传。
- [x] 6.2 后端接入 COS：新增 `worker/src/lib/cos.js`（COS PUT 预签名），新增「获取视频上传签名」接口 `POST /api/admin/projects/:id/video/sign`，COS 配置走 secrets。
- [x] 6.3 后台上传视频：项目编辑里选文件 → 前端用签名直传 COS（带进度）→ 把 COS 地址写入项目 `video` 字段；保留手填外链。
- [x] 6.4 文档与验证：更新 DEPLOY / TECH_STACK / DATA_MODEL / PROGRESS，线上验证上传与播放（预签名 200 → 直传 200 → 公有读 200 → 删除 204）。

## Phase 4：验证

- [x] 4.1 运行 `npm run build`，确认前端构建成功。
- [ ] 4.2 编写或引入最小 API 测试，覆盖注册、登录、留言和管理员权限。
- [ ] 4.3 手工验证核心页面和接口闭环。
- [ ] 4.4 确认生产环境环境变量和持久化目录配置正确。

## Phase 5：部署

- [x] 5.1 确定前端静态托管平台：Cloudflare Pages（连 GitHub 自动构建）。
- [x] 5.2 确定 API 托管方案：Cloudflare Workers + Hono + D1 + KV（全免费，KV 免费）。
- [x] 5.3 后端从 Node/Express/SQLite 重写为 Workers/Hono/D1/KV，并模块化（`worker/src/lib`、`worker/src/routes`）。
- [x] 5.4 一键切换后端：`scripts/backend.mjs` + `scripts/dev.mjs`，Vite 自动代理到检测到的后端端口。
- [x] 5.5 配置 SPA 回退（`public/_redirects`）与可选同源代理（`functions/api/[[path]].js`）。
- [x] 5.6 归档 Node 后端到桌面 `OwnerWeb-backend-node/`，从项目移除 `server/`（保留可回放）。
- [x] 5.7 清理敏感信息并推送 GitHub（origin = `https://github.com/litt-s/OwnerWeb.git`，Gitee 保留为 `gitee`）。
- [ ] 5.8 在 Cloudflare 创建 D1/KV、配置 secrets 并 `npx wrangler deploy`。
- [ ] 5.9 配置 Pages 环境变量（`VITE_API_BASE` 或 `API_ORIGIN`）与自定义域名 HTTPS。
- [ ] 5.10 上线后按 `DEPLOY.md` 的验证清单逐项检查。
